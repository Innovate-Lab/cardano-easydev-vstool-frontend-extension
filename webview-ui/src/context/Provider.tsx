// import React, { useState } from "react";
// import { createContext, useContext } from "react";

// export type  = {
  
// };

// const  = createContext< | null>(null);

// export const : React.FC<{ children: React.ReactNode }> = ({ children }) => {
 

//   return (
//     <.Provider
//       value={}>
//       {children}
//     </.Provider>
//   );
// };

// export const useMySuiEnv = () => {
//   const context = useContext();
//   if (context === undefined) {
//     throw new Error("useMySuiEnv must be used within a ");
//   }
//   return context as ;
// };
